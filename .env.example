# =============================================================================
# TUM Akıllı Ders Asistanı - Ortam Değişkenleri
# =============================================================================
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun:
#   cp .env.example .env
# =============================================================================

# OpenAI API Key (zorunlu)
# https://platform.openai.com/api-keys adresinden alabilirsiniz
OPENAI_API_KEY=sk-your-api-key-here

# Embedding modeli (opsiyonel, varsayılan: text-embedding-3-small)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Chat modeli (opsiyonel, varsayılan: gpt-4o-mini)
OPENAI_CHAT_MODEL=gpt-4o-mini

# ChromaDB ayarları (opsiyonel)
CHROMA_PERSIST_DIR=data/chroma
CHROMA_COLLECTION_NAME=tum_courses
